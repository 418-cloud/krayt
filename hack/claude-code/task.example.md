# Task

Add a `hello()` function to this repository that prints "hello from krayt" and a short unit
test for it, in whatever language the repo already uses. Keep the change small and idiomatic,
and update the README if there is one.

When you are done, briefly summarize what you changed and why.
